name: afrobeat_player
description: Afrobeat Music Player App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.25.0
  firebase_auth: ^4.14.0
  just_audio: ^0.9.25
  flutter_equalizer: ^1.0.0
  on_audio_query: ^2.5.0
  permission_handler: ^11.0.1
  dio: ^5.3.2
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
